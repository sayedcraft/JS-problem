function getMax(num1, num2) {
  // Math.max ফাংশন ব্যবহার করে দুটি সংখ্যার মধ্যে বড় সংখ্যাটি বের করা হলো
  return Math.max(num1, num2);
}

console.log(getMax(10, 25)); // Output: 25
console.log(getMax(42, 7));  // Output: 42